---
title: "Simple Matplotlib Task"
---

# 📊 Simple Matplotlib Test

Modify the Python code below and click **Run Code** to visualize the plot.

## **📝 Task Instructions**
1. Modify the `x` and `y` values.
2. Change the **title** and **axis labels**.
3. Try different **line styles** (`'-'`, `'--'`, `':'`, etc.).
4. Click **Run Code** to generate the plot.

---

import PythonRunner from "../../../components/PythonRunner";

<PythonRunner initialCode={`
x = [0, 1, 2, 3, 4, 5]
y = [i**2 for i in x]

ax.plot(x, y, marker='o', linestyle='-', color='b', label="Quadratic Growth")
ax.set_xlabel("X Values")
ax.set_ylabel("Y Values (Squared)")
ax.set_title("Simple Line Plot with Matplotlib")
ax.legend()
`} />
