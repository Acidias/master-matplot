---
title: "Simple Plot"
---

# Simple Matplotlib Plot

Below is a simple **Matplotlib** example that runs inside Nextra Docs.

## Task Instructions
1. Observe the Python code below.
2. Update the `x` and `y` values.
3. See the output below.

## Code Execution

import PythonRunner from "../../../components/PythonRunner";

<PythonRunner code={`
import matplotlib.pyplot as plt

x = [0, 1, 2, 3, 4]
y = [i**2 for i in x]

plt.plot(x, y, marker='o')
plt.xlabel("X-axis")
plt.ylabel("Y-axis")
plt.title("Simple Matplotlib Plot")
plt.show()
`} />
